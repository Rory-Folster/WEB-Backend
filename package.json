{
  "name": "rory-backend",
  "version": "1.0.0",
  "description": "API created for WEB602",
  "main": "index.js",
  "scripts": {
    "build": "cd backend && npm install  && cd ../frontend && npm install && npm run build",
    "start": "node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rory Folster",
  "license": "MIT"
}
